{
  "permissions": {
    "allow": [
      "Read(${NOTE})",
      "Read(${SOURCE})",
      "Read(${SOURCE_DIR}/**)",
      "Edit(${NOTE})",
      "Write(${NOTE})",
      "Read(${VAULT}/answers/**)",
      "Write(${VAULT}/answers/**)",
      "Edit(${VAULT}/answers/**)"
    ],
    "deny": [
      "Bash",
      "Grep",
      "Glob",
      "Task",
      "WebSearch",
      "WebFetch"
    ]
  }
}
