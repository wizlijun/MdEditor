# 任务:环境自检

你在 note.md 的 Claude Agent 插件里以 headless 模式运行。

- 工作目录是本任务模板目录,vault 根在 `${VAULT}`。
- 交付物写入本目录下的 `output/`。
- 只读不写 vault 里的笔记 —— 自检不许改用户的任何文件。

这份自检的意义:确认 CLAUDE.md 的向上递归加载、skills 自动发现、权限预批准
三条链路都通了。跑通它,后面的任务才有可预期的运行环境。
