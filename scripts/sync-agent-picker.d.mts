// Types for the copy-sync script, so the drift test can import it under
// `checkJs`-less strictness without loosening the project's settings.
export declare const FILES: string[]
export declare const TARGETS: string[]
export declare function canonical(name: string): string
